/**
 * This phase handle with search process, it
 * choose a random neighbour from a neighborhood
 */
const searchPhase = (neighborhood) => {
  const neighbour = [];
  return neighbour;
};

export { searchPhase };
